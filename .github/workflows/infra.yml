name: Infra Deployment

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  infra:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: infra

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan -var-file="terraform.tfvars"

      - name: Terraform Apply — infra + ALB controller
        # Applies everything except Gateway + HTTPRoute.
        # Gateway needs the ALB controller running and GatewayClass registered first.
        run: |
          terraform apply -auto-approve -var-file="terraform.tfvars" \
            -target=azurerm_resource_group.main \
            -target=azurerm_virtual_network.main \
            -target=azurerm_subnet.aks \
            -target=azurerm_subnet.alb \
            -target=azurerm_kubernetes_cluster.aks \
            -target=azapi_resource.alb \
            -target=azapi_resource.alb_association \
            -target=helm_release.alb_controller \
            -target=kubernetes_namespace.app \
            -target=kubernetes_deployment.nginx \
            -target=kubernetes_service.nginx

      - name: Get AKS credentials
        run: |
          az aks get-credentials \
            --resource-group rg-aks-alb-poc1 \
            --name aks-alb-poc1 \
            --overwrite-existing

      - name: Install Gateway API CRDs
        run: |
          kubectl apply -f https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.2.0/standard-install.yaml
          kubectl wait --for=condition=Established \
            crd/gateways.gateway.networking.k8s.io \
            crd/httproutes.gateway.networking.k8s.io \
            --timeout=120s

      - name: Wait for ALB controller to be Ready
        run: |
          DEPLOY=$(kubectl get deployment -n azure-alb-system \
            -o jsonpath='{.items[0].metadata.name}' 2>/dev/null)
          echo "ALB controller deployment: $DEPLOY"
          kubectl rollout status deployment/"$DEPLOY" \
            -n azure-alb-system \
            --timeout=300s

      - name: Wait for GatewayClass to be Accepted
        # Controller takes ~10-30s after pod Ready to register the GatewayClass.
        # Gateway apply before this causes a 10-min timeout.
        run: |
          kubectl wait gatewayclass azure-alb-external \
            --for=condition=Accepted \
            --timeout=120s

      - name: Terraform Apply — Gateway + HTTPRoute
        run: terraform apply -auto-approve -var-file="terraform.tfvars"
